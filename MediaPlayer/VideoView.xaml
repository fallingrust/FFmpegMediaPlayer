<UserControl x:Class="MediaPlayer.VideoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MediaPlayer" xmlns:interop="clr-namespace:System.Windows.Interop;assembly=PresentationCore"
             mc:Ignorable="d"           
            Background="Black"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Image Name="image" Stretch="Fill" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Image.Source >
                <interop:D3DImage x:Name="d3dImg"/>
            </Image.Source>
        </Image>
    </Grid>
</UserControl>
